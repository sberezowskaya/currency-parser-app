# 💰 Проект: Парсер Курсов Валют (Теория и Практика Многопоточности)

Spring Boot приложение для парсинга актуальных курсов валют с официального сайта ЦБ РФ с веб-интерфейсом и REST API.
## Общая Логика

1.  Парсинг: Используется WebFlux (WebClient) для получения XML-данных с ЦБ РФ

2. Хранение: Встроенная БД H2 для хранения результатов

3. Многопоточность:

- Планировщик: Аннотация @Scheduled для автоматического запуска каждые 30 минут

- Обработка: Каждая валюта обрабатывается в отдельном потоке из кастомного ExecutorService (FixedThreadPool на 4 потока)

4. Извлечение: Получение результатов из H2 с сортировкой через parallelStreamApi

🌐 Использование

Веб-интерфейс
- Конвертер валют - перевод рублей в другие валюты

- Таблица курсов - просмотр актуальных курсов

- Автообновление - данные обновляются каждые 30 минут

### 1. Запуск Проекта

Проект не требует дополнительных настроек (все в `application.properties`).
Запустите главный класс `Main.java` или выполните команду:

`./mvnw spring-boot:run`

После запуска откройте: http://localhost:8080
